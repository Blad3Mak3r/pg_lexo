-- pg_lexo 0.6.0 SQL
-- This file documents the new architecture without the lexo schema.
-- All type and function definitions are auto-generated by pgrx.

-- ============================================================================
-- NEW IN 0.6.0: Architecture Changes
-- ============================================================================
-- The extension now uses the default schema instead of a separate `lexo` schema.
-- This avoids pgrx bug #2134 with schema-qualified operator classes.
--
-- New type: lexo (no schema qualification needed)
-- With operators: =, <>, <, <=, >, >=
-- With btree and hash operator classes for indexing
--
-- ============================================================================
-- BREAKING CHANGES FROM 0.5.0
-- ============================================================================
-- Type renamed: lexo.lexorank -> lexo
-- Functions renamed:
--   - lexo.first() -> lexo_first()
--   - lexo.after(lexo.lexorank) -> lexo_after(lexo)
--   - lexo.before(lexo.lexorank) -> lexo_before(lexo)
--   - lexo.between(lexo.lexorank, lexo.lexorank) -> lexo_between(lexo, lexo)
--   - lexo.next(text, text, text, text) -> lexo_next(text, text, text, text)
--   - lexo.add_lexo_column_to(text, text) -> lexo_add_column(text, text)
--   - lexo.rebalance(text, text, text, text) -> lexo_rebalance(text, text, text, text)
--
-- ============================================================================
-- FUNCTIONS
-- ============================================================================
-- Main functions:
--   - lexo_first() -> lexo
--   - lexo_after(lexo) -> lexo
--   - lexo_before(lexo) -> lexo
--   - lexo_between(lexo, lexo) -> lexo
--   - lexo_next(text, text, text, text) -> lexo
--   - lexo_add_column(text, text) -- Creates lexo columns
--   - lexo_rebalance(text, text, text, text) -> bigint
--
-- ============================================================================
-- EXAMPLE USAGE
-- ============================================================================
-- Using the new lexo type:
--
-- CREATE TABLE items (
--     id SERIAL PRIMARY KEY,
--     position lexo NOT NULL
-- );
--
-- INSERT INTO items (position) VALUES (lexo_first());
-- INSERT INTO items (position) VALUES (lexo_after(lexo_first()));
--
-- SELECT * FROM items ORDER BY position;  -- Built-in ordering!
--
-- ============================================================================
