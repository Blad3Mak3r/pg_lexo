-- pg_lexo upgrade from 0.4.3 to 0.5.0
-- 
-- This migration adds the new LexoRank custom PostgreSQL type and
-- provides backwards-compatible TEXT functions.
--
-- IMPORTANT: After running this migration, you need to:
-- 1. Run `ALTER EXTENSION pg_lexo UPDATE TO '0.5.0';` in psql
-- 2. The new LexoRank type and functions will be available
--
-- Note: Existing TEXT columns will continue to work with the new
-- _text variants of functions (lexo.first_text, lexo.after_text, etc.)
--
-- To migrate existing TEXT columns to LexoRank, see the migration guide
-- in sql/pg_lexo--0.5.0.sql

-- The actual type and function definitions are auto-generated by pgrx
-- during the extension upgrade process.
